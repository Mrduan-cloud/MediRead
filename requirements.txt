# =========== Web / API ===========
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1
python-multipart==0.0.9
PyJWT==2.8.0

# =========== Agent / LLM ===========
langchain==0.1.16
langchain-core==0.1.46
langchain-community==0.0.34
openai==1.23.2

# =========== OCR ===========
# PaddleOCR 2.7+ 引入了 PP-OCRv4 模型(中文识别 + 检测精度均显著提升),
# 这是医疗体检报告解析依赖的最小版本。3.0+ 是 PP-OCR 主架构重写
# (breaking 接口 + 新模型权重格式),先 cap 在 2.x 线避免被自动升级到 3.x
# 后报告解析 Agent 静默退化。需要升 3.x 时按独立任务对齐。
paddleocr>=2.7.3,<3.0.0
# paddlepaddle 2.5+ 是 PaddleOCR 2.7+ 的最低运行时,同样 cap 在 2.x 线
# (3.x 改了算子注册机制 + 动态图默认行为,需要逐步迁移)
paddlepaddle>=2.6.1,<3.0.0
pdf2image==1.17.0
Pillow==10.3.0
numpy==1.26.4

# =========== RAG / Vector / Rerank ===========
pymilvus==2.4.0
sentence-transformers==2.6.1
FlagEmbedding==1.2.10
rank-bm25==0.2.2

# =========== Storage / DB ===========
tortoise-orm==0.20.0
asyncmy==0.2.9
redis==5.0.3
minio==7.2.5

# =========== Observability ===========
loguru==0.7.2
prometheus-client==0.20.0
tenacity==8.2.3

# =========== Utils ===========
httpx==0.27.0
python-dotenv==1.0.1
jsonschema==4.21.1
